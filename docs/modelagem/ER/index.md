import RequisitosTable from '@site/src/components/RequisitosTable';

# Modelo de Entidades (ER)

Esta seção apresenta o **modelo entidade-relacionamento** do domínio, incluindo
diagramas (Mermaid/PlantUML) e a descrição das entidades.

<RequisitosTable type="ER" />

> Será derivado do domínio canônico em `Existentia.Lore`. O detalhamento fica em
> páginas individuais (`unlisted: true`), acessíveis apenas pelos links acima.
